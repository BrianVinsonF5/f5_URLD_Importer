# bigip-urldb-sync — Python dependencies
# Install with: pip install -r requirements.txt

# HTTP client — used for iControl REST calls and remote URL source fetching
requests>=2.31.0

# CLI framework
click>=8.1.0

# Web service framework
fastapi>=0.111.0

# ASGI server for the FastAPI app
uvicorn[standard]>=0.29.0

# Settings management via environment variables / .env files
pydantic-settings>=2.2.0

# .env file loading (used by pydantic-settings; also available standalone)
python-dotenv>=1.0.0

# Async HTTP client — used by FastAPI internally and available for future async BIG-IP calls
httpx>=0.27.0

# Optional: scheduled job support (reserved for future use / advanced deployments)
# The current implementation uses systemd timers for scheduling; APScheduler is
# included here for projects that prefer in-process scheduling.
apscheduler>=3.10.0
